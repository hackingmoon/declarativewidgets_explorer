# All files under this package directory are distributed under the terms of
# the Modified BSD License.

"""
Only declaring this directory as a Python package.

To make the directory contents directly installable as ``package_data`` of a
Python package redirected to this via ``package_dir`` in a ``setup.py`` of a
Python project that wants to bundle ``declarativewidgets_explorer`` in a
simple way
"""
